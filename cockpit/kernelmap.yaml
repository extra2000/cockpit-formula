# -*- coding: utf-8 -*-
# vim: ft=yaml

Linux:
  packages:
    - cockpit
    - pcp
  services:
    - pmcd.service
    - pmlogger.service
    - cockpit.socket
  dashboard:
    packages:
      - cockpit-dashboard
  machines:
    packages:
      - cockpit-machines
