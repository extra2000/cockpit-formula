# -*- coding: utf-8 -*-
# vim: ft=yaml

Arch: {}

Debian: {}

Suse:
  firewall:
    package: firewalld

RedHat:
  dashboard:
    packages:
      - cockpit-dashboard.noarch
  firewall:
    package: firewalld
